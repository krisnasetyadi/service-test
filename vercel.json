{
    "version": 2,
    "builds": [
      {
        "src": "dist/main.ts",
        "use": "@vercel/node",
        "config": { "outputDirectory": "public" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "dist/main.ts", "methods": ["GET", "POST"] }
    ]
  }
  